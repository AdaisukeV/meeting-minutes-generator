# CLAUDE.md

このリポジトリは、Claude Code(CLI)を利用して会議の議事メモ・録画から議事録(Markdown)を自動生成するアプリケーションである。

**実装・仕様判断に迷ったら、まず `docs/requirements.md`(要件定義書)を参照すること。** このファイルには要件定義書に書かれた内容を繰り返し記載しない。ここに書くのは、コード全体に共通する実装ルール・注意事項のみとする。

> **適用範囲に関する重要な注記**
> このファイルは**開発時**(対話モードでアプリのコードを書く・直すセッション)にのみ適用される。
> `generate_minutes.sh` が実行する議事録生成パイプライン(ステップ1〜3)は `claude -p --bare --system-prompt-file projects/{name}/prompts/*.md` のようにシステムプロンプトを完全に置き換え、かつ `--bare` とリポジトリ外の空ディレクトリでの実行によって本ファイルの自動読み込みを無効化した状態で非対話実行されるため、本ファイルの内容は運用時の議事録生成処理には適用されない(`--system-prompt-file` 単独では混入する。詳細は `docs/requirements.md` 3.2.5・3.2.7を参照)。運用時のプロンプト(議事メモの整理・構造化・フォーマットに関する指示)を編集する場合は、本ファイルではなく `projects/{name}/prompts/` 配下を編集すること。

---

## 1. 絶対に守るべき制約(最優先)

- **`inputs/`・`outputs/`・`config/auth/active.env` を絶対にGitへコミットしない。** これらには機密情報(会議内容・認証情報)が含まれる。ファイル作成・編集時にこれらのパス配下を扱う場合、`.gitignore` に含まれていることを都度確認する。
- **音声・動画の生データを外部(API・クラウド)に送信するコードを書かない。** 文字起こしはローカル(`faster-whisper`)で完結させ、Claude Codeに渡すのはテキスト化後のデータのみとする。
- **Web検索・外部APIへの問い合わせを行う機能を追加しない。** 本アプリはローカルファイルのみを扱う設計であり、機密情報を外部に送信する経路を増やさない。
- **認証情報(APIキー・AWSアクセスキー等)をコード中にハードコードしない。** 認証は必ず `config/auth/active.env` 経由、またはAWS SSOセッション経由で取得する。

---

## 2. アーキテクチャ概要

```
CLAUDE.md                # 本ファイル(共通ルール)
docs/requirements.md     # 要件定義書(仕様の正)
docs/design.md            # 設計書ハブ(位置づけ・全体アーキテクチャ・ファイル構成・分冊索引・要件対応表)
docs/design/              # 設計書の分冊(01-conventions.md 〜 09-decisions.md:実装のインターフェース・スキーマ・テスト戦略)
docs/memo/                # コメントアウト・処理削除・ライブラリ変更の記録
config/auth/              # 認証経路の抽象化(active.envのみGit管理外)
projects/{name}/
  config.yaml             # 話者・会社名の定義
  prompts/
    01_organize.md         # ステップ1用システムプロンプト
    02_structure.md        # ステップ2用システムプロンプト
    03_format.md            # ステップ3用システムプロンプト
scripts/
  transcribe.py            # 音声・動画 → テキスト(ローカル文字起こし)
  aws_sso_login.sh          # Bedrock利用時のSSOログイン
  generate_minutes.sh       # 3ステップパイプラインの実行エントリポイント
.claude/commands/
  minutes-review.md         # 議事録レビュー用スラッシュコマンド
  save-design.md            # 実装と設計書の差分を該当分冊へ反映するスラッシュコマンド
inputs/{project}/{date}/    # ★Git管理外
outputs/{project}/{date}/   # ★Git管理外(中間生成物3ファイル+最終議事録)
```

### 議事録生成は3段階パイプライン

議事メモ・文字起こしから一度に議事録を生成しない。以下の順で、**各ステップを独立した処理として実装・実行する**(1回のプロンプトで3段階すべてを済ませようとしない)。

1. **話者・発言の整理** → `outputs/{project}/{date}/01_speakers_utterances.md`
2. **会話の構造化**(トピック・応答関係) → `outputs/{project}/{date}/02_structured_conversation.md`
3. **議事録フォーマット** → `outputs/{project}/{date}/minutes.md`

各ステップは、前段の出力ファイルを入力として明示的に読み込む形で実装する。ステップ間の処理を暗黙的に一括化するコードは書かない(部分的な再実行・修正を可能にするため)。

---

## 3. コーディング規約

- **スクリプトはBash / Python を基本とする**(`scripts/`配下)。新規言語・フレームワークの追加は要件定義書に記載がない限り行わない。
- **シェルスクリプトは `set -euo pipefail` を先頭に必ず入れる。**
- **Python依存関係は `requirements.txt` に追記し、`pip install --break-system-packages` を前提にしない**(devcontainer側で仮想環境またはユーザーインストールを想定)。
- **設定値のハードコードを避ける。** プロジェクト固有の値(話者名・会社名・議事録構成・モデルID等)は必ず `projects/{name}/config.yaml` または `prompts/*.md` に外出しする。
- **エラー時は握りつぶさず、原因が分かるメッセージを標準エラー出力に出して終了する。** 特に認証エラー・ファイル未検出エラーは、利用者が次に何をすべきか分かる文言にする。
- **中間生成物・最終出力はMarkdown形式で統一する。** JSON等の厳密な構造化形式は採用しない(理由は `docs/requirements.md` 3.2.6を参照)。将来的な構造化ニーズはYAML Front Matterでの拡張を検討する。

---

## 4. 認証・実行環境に関する注意

- 認証経路(Bedrock / 商用APIキー / 将来のVertex等)は `config/auth/active.env` によって切り替わる。**スクリプト側に認証経路ごとの分岐ロジックを増やさない。** 環境変数を読み込むだけの実装を維持する。
- Bedrock利用時のみ `aws_sso_login.sh` が呼ばれる。**新しい認証方式を追加する際も、既存の呼び出し元(`generate_minutes.sh`)の分岐を複雑にせず、`active.env` の内容で自然にスキップされる設計を踏襲する。**
- ローカル環境(`active.env`)とCodespaces環境(Secrets経由の環境変数)の両方で動くよう、**「`active.env` が存在すれば読み込み、なければ既存の環境変数をそのまま使う」というフォールバックを崩さない。**
- Bedrock利用時のモデルIDは環境変数で明示的に固定する想定。**モデルのエイリアス(`sonnet`等)をコード中に直接書かない。**

---

## 5. 変更を加える際の進め方

- 実装前に、対象の変更が `docs/requirements.md` のどのセクションに対応するかを確認する。要件に記載のない機能を推測で追加しない。
- 実装は `docs/design.md`(設計書ハブ)を起点に行う。ハブの分冊索引から**これから書くファイルに対応する分冊のみ**を読み(全分冊を読む必要はない)、併せて `docs/design/01-conventions.md`(共通規約)を必ず参照する。設計書に対象の設計が無い、または実態と乖離している場合は、先に該当分冊を更新してから実装する。
- 1つの変更は1つのファイル・1つの関心事に閉じるよう心がける(レビュー・ロールバックのしやすさを優先)。タスクは最長30分程度の単位に分割する。
- 新しいディレクトリ・ファイルを追加する場合は、`docs/requirements.md` の「4. アプリ構成」のディレクトリ構造と矛盾しないか確認する。構造を変更する場合は、要件定義書側の更新も提案する。
- テスト・動作確認は、`inputs/` にダミーデータを配置して `scripts/generate_minutes.sh` を実行する形で行う(機密性の高い実データはコミット・共有しない)。
- 実装が設計書と乖離した場合は差分を検出し、該当する `docs/design/` の分冊を最新化する(`/save-design` コマンドの利用を推奨)。新しい関心事はまず既存の分冊へ追記し、分冊の追加は肥大化した場合にのみ提案する。分冊構成を変えた場合は `docs/design.md` の分冊索引と要件対応表も更新する。
- 以下の変更を行った場合は必ず `docs/memo/` に記録する:コメントアウト、処理削除、ライブラリ(依存パッケージ)の変更。
- 依存パッケージの追加は最小限にし、追加時は標準機能・既存依存で代替できないかを先に検討する。
- 実行コマンドはCI/CD用(非対話・非ページャ)を使用する(例:`git --no-pager`、`gh`のpager無効化)。

---

## 6. TDDの適用方針

本アプリは「決定的なコード(スクリプトのグルーロジック)」と「非決定的な処理(LLMによる議事録生成そのもの)」が混在するため、TDD(Kent Beck準拠、Red→Green→Refactor)の適用範囲を以下のように区別する。詳細は `docs/requirements.md` 8.3を参照。

| 対象 | TDDの適用 |
|---|---|
| 引数パース、`.env`読み込み、ファイルパス生成、`--from-step`制御、SSOセッション有効性チェック等 | **通常のTDDを適用** |
| `transcribe.py`の呼び出しロジック | **呼び出し部分はTDD対象、文字起こし精度は対象外** |
| ステップ1〜3の `claude -p` 呼び出し(議事録生成本体) | **呼び出しをラップする関数はTDD対象、生成内容の品質は対象外**(`/minutes-review`・人間レビューで担保) |

- 処理を変更するたびにテストを実行し、常にグリーンな状態を保つ。Formatter/Linterも常に通す。
- 使用ツール:Python は `pytest`(テスト)/ `ruff`(lint)/ `black`(format)、Bash は `bats`(テスト)/ `shellcheck`(lint)/ `shfmt`(format)。
- カバレッジは決定的なコード部分に対して80%以上を目標とする(プロンプト・LLM生成部分は対象外)。
- タスクの完了条件:デグレードがないこと、Format/Lint/Testが一貫して成功していること、決定的なコード部分のカバレッジが80%以上であること。
